# Test Properties for ConfigUtils tests
test.key=test-value
another.key=another-value

# Kafka Configuration
bootstrap.servers=test-bootstrap-servers
security.protocol=SASL_SSL
sasl.jaas.config=org.apache.kafka.common.security.plain.PlainLoginModule required username='test-username' password='test-password';
sasl.mechanism=PLAIN
client.dns.lookup=use_all_dns_ips
session.timeout.ms=45000
acks=all

# Topic Configuration
topic.name=test-topic
topic.test=test-topic-name
topic.flights=test-flights-topic

# Table Configuration
table.test=TestTable
table.flights=TestFlights
